<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.equipment.mapper.SysEquipmentAdminMapper">

    <select id="getByAdminId" resultType="java.lang.String">
        select equipment_id from sys_equipment_admin where admin_id = #{id}
    </select>
</mapper>
